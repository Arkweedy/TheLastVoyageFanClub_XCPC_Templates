假设每个元素有个三个属性，$x[i]$，$y[i]$，$i$，问题即找最大的 $j$ 使得 $x[i] > x[i]$  且 $y[i] > y[j]$ 且 $j<i$ 。

使用线段树上二分可以在 $O(nlogn)$ 的时间内对每个点求解这个前驱：

首先按照 $x$ 从小到大排序，以 $i$ 为线段树的下标，动态插入 $y[i]$， 维护已经扫描过的 $i$ 对应 $y[i]$ 区间最小值，然后在线段树上二分。